# Only needed for standalone `graphify extract`/`graphify query` runs outside
# an AI-assistant session (Claude Code reuses its own configured key).
# Copy to .env (gitignored) and fill in whichever backend you use.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
